
{{> head}}
{{> header}}

<article class="bg-body-tertiary border rounded-3 p-4">
  <div class="d-flex flex-column flex-md-row justify-content-between gap-2">
    <div>
      <h1 class="h3 mb-1">{{course.title}}</h1>
      <p class="text-secondary mb-2">
        Level: {{course.level}} • Type: {{course.type}} {{#course.allowDropIn}}• Drop-in available{{/course.allowDropIn}}
      </p>
    </div>
    <div class="text-md-end">
      <a class="btn btn-primary" href="/courses/{{course.id}}/book">Book this course</a>
    </div>
  </div>

  <p class="mb-2">{{course.description}}</p>
  <p class="text-secondary mb-1">Dates: {{course.startDate}} – {{course.endDate}}</p>
  <p class="text-secondary mb-1">Location: {{course.location}}</p>
  <p class="text-secondary mb-0">Price for full course: £{{course.price}}</p>

  <h2 class="h5 mt-4 mb-2">Sessions</h2>
  <div class="table-responsive">
    <table class="table table-sm table-hover align-middle mb-0">
      <thead>
        <tr>
          <th>Start</th>
          <th>End</th>
          <th>Location</th>
          <th class="text-end">Price</th>
          <th class="text-end">Capacity</th>
          <th class="text-end">Booked</th>
          <th class="text-end">Remaining</th>
          <th class="text-end"></th>
        </tr>
      </thead>
      <tbody>
        {{#sessions}}
        <tr>
          <td>{{start}}</td>
          <td>{{end}}</td>
          <td>{{location}}</td>
          <td class="text-end">£{{price}}</td>
          <td class="text-end">{{capacity}}</td>
          <td class="text-end">{{booked}}</td>
          <td class="text-end">{{remaining}}</td>
          <td class="text-end">
            {{#../course.allowDropIn}}
              {{#remaining}}
                <a class="btn btn-outline-primary btn-sm" href="/sessions/{{id}}/book">Book drop-in</a>
              {{/remaining}}
              {{^remaining}}
                <span class="text-secondary">Fully booked</span>
              {{/remaining}}
            {{/../course.allowDropIn}}
            {{^../course.allowDropIn}}
              <span class="text-secondary">Drop-ins disabled</span>
            {{/../course.allowDropIn}}
          </td>
        </tr>
        {{/sessions}}
      </tbody>
    </table>
  </div>
</article>

{{> footer}}
